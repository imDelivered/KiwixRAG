# Minimal requirements for chatbot GUI
# Python standard library only - no external dependencies needed
# tkinter comes with Python (may need: sudo apt install python3-tk)
libzim
sentence-transformers
faiss-cpu
rank_bm25
numpy
beautifulsoup4
tqdm
requests
ollama
